import express from 'express';
const router = express.Router();


// router.get('/me'); //user request own profile
// router.put('/me'); // user updates its request
// router.get('/user-list');//admin req all users list with pagination 
// router.get('/:id') //admin req specific user profile
// router.put('/:id'); //admin updats user profile role
// router.delete('/:id'); //admin deletes user profile

export default router;