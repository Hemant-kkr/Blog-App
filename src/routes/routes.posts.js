import express from 'express';
const router = express.Router();


// router.get('/posts-list');//List published posts (pagination, filter by tag/category, search, sort)
// router.get('/:slug'); //Get single post by slug (increment views)
// router.post('/new-post');//Create new post
// router.put('/:id');//edit posts 
// router.patch('/:id');//publish unpublish 
// router.delete('/:id');//delete an post
// router.post('/:id/like');//like unlike post
// router.get('/top')//top posts by likes or comments or veiws
// router.get('/trending')//Trending posts (algorithm: recent + likes + views)
// router.get('/recent')//recents posts
// router.get('/category/:category')//serving posts by category
// router.get('/search')//serching posts by any keywords

export default router;