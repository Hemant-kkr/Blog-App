import express from 'express'
const router = express.Router();

// router.get('/post/:id');//Get views, likes, comments count for both admin and author
// router.get('/user/:id');//User activity analytics for admin only
// router.get('/top-authors')//Top authors by posts/engagement
// router.get('/top-posts')//top posts till now

export default router;
