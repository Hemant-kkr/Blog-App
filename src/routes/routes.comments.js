import express from 'express';
const router = express.Router();

// router.get('/post/:id')//List comments for a post (pagination)
// router.post('/post/:id')//post a comment on a blog post 
// router.delete('/:id')//delete an comment 
// router.put('/:id')//edit an comment

export default router;